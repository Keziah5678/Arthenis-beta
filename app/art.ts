/**
 * The one painted asset the product itself ships: the landing backdrop.
 *
 * World artwork is no longer drawn from a shared photo library. A stock image
 * is out of context in most worlds, so an entity without a generated image now
 * gets a placeholder built from its own world's visual identity instead.
 */
export const HERO_IMAGE = "/art/hero.jpg";
