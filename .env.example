NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
OPENAI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: override the OpenAI models used by lib/ai/config.ts.
# Defaults (gpt-4o / gpt-image-1) are used if these are left empty.
# Check which models your OpenAI account can access and set these
# if you need a different one.
OPENAI_TEXT_MODEL=
OPENAI_IMAGE_MODEL=
